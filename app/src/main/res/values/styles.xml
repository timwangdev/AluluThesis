<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowFullscreen">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="AppTheme.ContentText" parent="Base.TextAppearance.AppCompat.Display2">
        <item name="android:fontFamily">@font/poiret_one</item>
        <item name="android:textSize">22sp</item>
        <item name="android:color">@color/colorText</item>
    </style>

    <style name="AppTheme.AppBarTitle" parent="Base.TextAppearance.AppCompat.Subhead">
        <item name="android:fontFamily">@string/medium_font</item>
        <item name="android:color">@color/colorText</item>
    </style>

</resources>
